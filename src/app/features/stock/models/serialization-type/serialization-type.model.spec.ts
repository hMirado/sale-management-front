import { SerializationType } from './serialization-type.model';

describe('SerializationType', () => {
  it('should create an instance', () => {
    expect(new SerializationType()).toBeTruthy();
  });
});
